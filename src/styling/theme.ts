const color = {
  dark: "#211d22",
  light: "#fefefe",
  black: "#070607",
  brown: "#5d4038",
  pink: "#dfaba0",
};

const space = {
  xs: "0.125rem",
  s: "0.5rem",
  m: "1rem",
  l: "2rem",
  xl: "4rem",
};

const border = {
  xs: `${space.xs} solid ${color.pink}`,
  xxs: `1px solid ${color.pink}`,
};

const responsive = {
  maxMobileWidth: "540px",
};

export const THEME = {
  color,
  space,
  border,
  responsive,
};
